<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bjtu.edu.orders_service.mapper.OrderMapper">
<!--    <resultMap id="BaseResultMap" type="Order">-->
<!--        <result column="id" jdbcType="INTEGER" property="id" />-->
<!--        <result column="uid" jdbcType="INTEGER" property="user.id" />-->
<!--        <result column="bid" jdbcType="INTEGER" property="book.id" />-->
<!--        <result column="date" jdbcType="VARCHAR" property="date" />-->
<!--    </resultMap>-->

    <select id="findAllByStateAndUserid" resultType="Order">
        select * from orders where state=#{state} and uid =#{userid}
    </select>

    <insert id="addOne" parameterType="Order">
        insert into orders (id,uid,bid,date) values(#{id},#{user.id},#{book.id},#{date})
    </insert>

    <select id="findById" parameterType="Integer" resultType="Order">
        select * from orders where id =#{id}
    </select>

    <delete id="deleteOne" parameterType="Integer">
        delete from orders where id=#{id}
    </delete>

    <update id="updateOne" parameterType="Order">
        update orders set id=#{id},bid=#{bid},uid=#{uid},state=#{state} where id=#{id}
    </update>


</mapper>

